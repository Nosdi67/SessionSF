{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        {% for categorie in categories %}
        <li> {{ categorie.nom }} <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">Modifier</a>--
        <a href="{{ path('categorie_delete',{'id': categorie.id}) }}">Supprimer</a></li><br>
        {% endfor %}
    </ul>

    <a href="{{ path('categorie_new')}}">Ajouter une categorie</a>
    <a href="{{ path('admin_home')}}">Page Admin</a>
</div>
{% endblock %}
