{% extends 'base.html.twig' %}

{% block title %}Hello StagiairesController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>


    <ul>
        {% for stagiaire in stagiaires %}
            <li> {{stagiaire.nom}} {{stagiaire.prenom}} <a href="{{ path('stagiaire_edit',{'id': stagiaire.id}) }}">Modifier</a>
            <a href="{{ path('stagiaire_info', {'id': stagiaire.id}) }}">Info</a></li>
        {% endfor %}
    </ul>
</div>
<a href="{{ path('stagiaire_new') }}">Ajouter un stagiaire</a>
{% endblock %}
